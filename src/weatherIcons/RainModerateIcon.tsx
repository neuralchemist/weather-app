import { ReactComponent as SVG } from "./rain-moderate.svg";
import styles from "./style.module.css";

function RainModerateIcon() {
  return <SVG className={styles.icon} />;
}

export default RainModerateIcon;
